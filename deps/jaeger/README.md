# Manual Compilation for jaeger-idl

Installation for `jaeger-idl`.

## Compilation

Run the following command

```bash
# Note: Update w/o recursive option
git submodule update --init

cd ./jaeger-idl && make proto
```

